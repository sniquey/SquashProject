<h1>Listing clubs</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Location</th>
      <th>Find out more</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @clubs.each do |club| %>
      <tr>
        <td><%= club.name %></td>
        <td><%= club.location %></td>
        <td><%= club.manager %></td>
        <td><%= link_to 'Club Page', club %></td>
      </tr>
    <% end %>
  </tbody>
</table>
